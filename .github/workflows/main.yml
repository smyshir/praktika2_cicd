name: CI/CD Pipeline

on:
  push:
    branches: [ main, development, release ]
    tags:
      - 'v*'   # реагирует на теги релизов, например v1.0
  pull_request:
    branches: [ main ]

jobs:
  # --- CI: сборка и тестирование ---
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pdoc

      - name: Run tests
        run: pytest -v

      - name: Generate documentation
        run: |
          mkdir -p docs
          pdoc --html . -o docs
          echo "📘 Documentation generated successfully."

  # --- CD: деплой после успешных тестов ---
  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/release' || startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Simulate deployment
        run: |
          echo "✅ Tests passed successfully."
          echo "🚀 Deploying application to staging server..."
          echo "--------------------------------------------"
          echo "Branch or tag: $GITHUB_REF"
          echo "Deployed version ready!"

          